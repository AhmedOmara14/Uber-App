<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.uberapp">

    <uses-permission android:name="android.permission.CALL_PHONE" />
    <uses-permission
        android:name="android.permission.INTERNAL_SYSTEM_WINDOW"
        tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.CALL_PHONE" />
    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="false"
        android:theme="@style/AppTheme">
        <activity android:name=".ui.yallah"
            android:theme="@style/map"
            ></activity>
        <activity
            android:name=".ui.cus_map"
            android:label="@string/title_activity_cus_map"
            android:theme="@style/map">

        </activity>
        <activity android:name=".ui.customer_signup" />

        <meta-data
            android:name="com.google.android.geo.API_KEY"
            android:value="@string/map_key" />

        <activity android:name=".ui.Register_driver" />
        <activity android:name=".ui.login_driver" />
        <activity android:name=".ui.login_customer" />
        <activity
            android:name=".ui.driver_map"
            android:label="@string/title_activity_driver_map"
            android:theme="@style/map"></activity>
        <activity android:name=".ui.customerordriver" />
        <activity
            android:name=".ui.MainActivity"
            android:theme="@style/splachscreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>